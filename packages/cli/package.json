{
  "name": "@unified-scanner/cli",
  "version": "1.0.0",
  "description": "Command-line interface for unified security scanner",
  "type": "module",
  "bin": {
    "unified-scanner": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@unified-scanner/core": "workspace:*",
    "commander": "^11.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.1",
    "cli-table3": "^0.6.3"
  },
  "devDependencies": {
    "tsup": "^8.0.1"
  }
}
